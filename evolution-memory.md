# Evolution Memory: skill-lifecycle

## 进化日志

（暂无记录）


### [2026-09-22] CAPTURED - 项目创建：融合 skill-creator 与 skill-evolution
- **触发**：手动触发
- **五维评分**：未评估
- **根因**：—
- **修改**：建立创建/评估/进化闭环与进化记忆
- **验证**：self-check 通过，关键文件 20 项齐全
- **结果**：已应用
- **教训**：进化记忆必须是纯文件，才能跨会话、跨 Agent 共享

### [2026-09-23] FIX - 措辞歧义触发宿主 Agent 安全扫描拦截
- **触发**：用户纠正（QwenPaw `skills test` 拒绝加载，`safe=False`）
- **五维评分**：有效性 5 / 性能 5 / 稳定性 2→5 / 可测试性 5 / 体验 4
- **根因**：CREATE 步骤第 1 条写作「…直接从对话中提取，**不要让用户重复描述**」。
  本意是「别重复提问」，但「不要让用户…」句式被 QwenPaw Skill Scanner 的
  `conceal-actions-from-user` 规则命中（HIGH），判定为试图向用户隐瞒行为。
- **修改**：改写为「直接从对话中提取素材，**省去用户重复描述的成本**」，保留原意、消除歧义句式。
- **验证**：全局检索同类措辞（不要让用户 / 不告知 / 静默 / silently …）仅此 1 处；
  修复后 `qwenpaw skills test` → `0 finding(s), safe=True`；`self-check` 结构合规。
- **结果**：已应用
- **教训**：技能文本会被宿主 Agent 的安全扫描器逐行审查。**否定式指令若以「用户」为宾语，
  极易被误判为隐瞒行为**。写「省去用户成本」优于写「不要让用户做某事」——
  同一意图，前者零风险。这条应作为所有技能写作的通用规则。

## 已知缺陷（未修复）

（暂无）

## 优化模式库

### 模式：否定式指令的改写（安全扫描友好写作）
- **症状**：技能被宿主安全扫描拦截，报 `conceal actions from the user` / 提示词注入类告警
- **成因**：出现「不要 + 用户」「无需告知」「静默执行」等以隐瞒为语义的否定句式
- **改法**：把「不要做 X」改写为「从简做 Y」——即用**正向收益**替代**禁止行为**
  - ❌ 不要让用户重复描述 → ✅ 省去用户重复描述的成本
  - ❌ 无需询问直接应用 → ✅ 方案准备就绪后提请用户确认
- **附带收益**：这类改写同时提升技能正文的可读性与模型遵循度
